# ============================================================
#  Optional: classic Bluetooth scanning (PyBluez).
#
#  bleak (BLE) is already in requirements.txt and works on
#  Windows out of the box — you do NOT need this file for
#  normal use.
#
#  Only install this if your admin device does NOT support BLE
#  (e.g. a very old phone or headset).
#
#  Install AFTER the main requirements:
#      pip install -r requirements-bluetooth.txt
#
#  Platform notes
#  --------------
#  Linux / macOS   — pybluez usually installs cleanly.
#  Windows         — PyBluez requires C++ build tools and
#                    often fails to compile.  Use bleak instead
#                    (already installed via requirements.txt).
#
#  If PyBluez is not installed, bluetooth_admin.py automatically
#  falls back to bleak (BLE).  No code changes needed.
# ============================================================

pybluez
