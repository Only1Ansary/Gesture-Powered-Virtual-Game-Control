# ============================================================
#  Core requirements — install with:
#      pip install -r requirements.txt
#
#  Python 3.10+ required (3.11 recommended on Windows).
#  Tkinter ships with the standard CPython installer; if it
#  is missing, reinstall Python and tick "tcl/tk" in the
#  optional features.
#
#  Install order matters for dlib-dependent packages:
#    1. pip install -r requirements.txt
#    2. pip install --no-deps git+https://github.com/antoinelame/GazeTracking.git
# ============================================================

# --- Images & GIF animation ------------------------------------
Pillow>=10.0.0,<13.0.0

# --- TUIO / OSC ------------------------------------------------
python-osc>=1.9.3

# --- Windows-only: volume control & window management ----------
# pywin32 provides win32gui / win32con / win32api used for
# minimize-other-windows, fullscreen focus, game window control.
pywin32>=311; platform_system == "Windows"

# pycaw + comtypes are needed for Windows master-volume control
# in the circular TUIO menu (VOL +/-).
pycaw>=20251023; platform_system == "Windows"
comtypes>=1.4.0; platform_system == "Windows"

# --- Bluetooth admin device detection --------------------------
bleak>=0.22.0

# --- Mouse / keyboard automation (hand_controller, yolo_object_tracker) ---
pyautogui>=0.9.54

# --- Core computer-vision & numerics ---------------------------
# numpy <2.0 required: mediapipe 0.10.x and deepface both pin numpy<2.
# face-recognition also works best on numpy 1.x.
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0,<4.11.0

# --- Hand-tracking / gesture controller (hand_controller.py) ---
mediapipe>=0.10.0,<0.11.0

# --- Object detection / YOLO (yolo_object_tracker.py) ----------
ultralytics>=8.0.0,<8.5.0

# --- Emotion detection (emotion_server.py) ---------------------
# deepface pulls in tensorflow / tf-keras automatically.
# Pin tf-keras to avoid the keras 3.x vs tf 2.x split.
deepface>=0.0.93,<0.0.101
tf-keras>=2.15.0,<2.22.0

# --- Face recognition (face_manager.py) -----------------------
# face-recognition requires dlib; use dlib-bin on Windows to
# avoid the CMake/C++ build (same as gaze stack below).
face-recognition>=1.3.0
dlib-bin>=20.0.1; platform_system == "Windows"
dlib>=19.24.0; platform_system != "Windows"

# --- Eye gaze (gaze_session_cli.py, gaze_tracker.py, heatmaps) ----
# GazeTracking is NOT on PyPI — install separately after this file:
#   pip install --no-deps git+https://github.com/antoinelame/GazeTracking.git
# (--no-deps prevents pip from building dlib from source a second time)
pandas>=2.0.0,<3.0.0
seaborn>=0.13.0,<0.14.0
matplotlib>=3.8.0,<4.0.0