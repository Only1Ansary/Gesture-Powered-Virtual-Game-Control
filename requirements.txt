# ============================================================
#  Core requirements — install with:
#      pip install -r requirements.txt
#
#  Python 3.10+ required (3.11 recommended on Windows).
#  Tkinter ships with the standard CPython installer; if it
#  is missing, reinstall Python and tick "tcl/tk" in the
#  optional features.
# ============================================================

# --- Images & GIF animation ------------------------------------
Pillow>=12.0.0

# --- TUIO / OSC ------------------------------------------------
python-osc>=1.9.3

# --- Windows-only: volume control & window management ----------
# pywin32 provides win32gui / win32con / win32api used for
# minimize-other-windows, fullscreen focus, game window control.
pywin32>=311

# pycaw + comtypes are needed for Windows master-volume control
# in the circular TUIO menu (VOL +/−).
pycaw>=20251023
comtypes>=1.4.0

# --- Bluetooth admin device detection --------------------------
# Works on Windows/macOS/Linux — no build tools required.
# Falls back to PyBluez (classic BT) if also installed.
bleak>=0.22.0

# --- Hand-tracking / gesture controller (gesture_controller.py) -
# Used by the VR bridge to translate webcam hand poses to VR input.
# Skip these two if you do NOT use gesture tracking / VR bridge.
opencv-python>=4.8.0
mediapipe>=0.10.0

# --- Eye gaze (gaze_session_cli.py, gaze_tracker.py, heatmaps) ----
# Uses: OpenCV (camera), antoinelame/GazeTracking (dlib landmarks → gaze ratios),
#       numpy/pandas/seaborn/matplotlib (heatmap PNG after session).
# Windows: install dlib via dlib-bin below, then GazeTracking WITHOUT pulling dlib again:
#     python -m pip install --no-deps git+https://github.com/antoinelame/GazeTracking.git
# (Listing gaze-tracking in this file makes pip build dlib from source on many setups.)
# Optional: install only gaze stack → pip install -r requirements-gaze.txt  (+ command above)
# Camera: default native resolution in config (gaze_capture_width/height = 0); forced HD breaks many cams.
dlib-bin>=20.0.1; platform_system == "Windows"
dlib>=20.0; platform_system != "Windows"
numpy>=2.0
pandas>=2.0
seaborn>=0.13
matplotlib>=3.8
